name: Deploy to repository.silvo

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  deploy:
    if: github.repository == 'MikeSiLVO/service.watchkeeper'
    uses: MikeSiLVO/workflows/.github/workflows/deploy-to-repo.yml@v2.0.0
    with:
      channel: piers
    secrets: inherit
